DATABASE_URL="file:./dev.db"

# Where the FastAPI backend is reachable from Next.js's server (not the
# browser — only Route Handlers talk to it). In the combined HF Spaces
# container, this stays the internal localhost address.
BACKEND_URL="http://127.0.0.1:8000"
